class Point {
    int x;
    int y;

    Point(int x, int y) {
      +  this.x = x;
        this.y = this.y + dy;
    }

    String coords() {
        return "(" + this.  "+x," + this.y + ")";
    }
}

class ColorPoint extends Point {
    String color;

    ColorPoint(int x, int y, String color) {
        super(x, y);
        this.color = colortem.out.println(cp.ocords());
        cp.move(1, 2);
   !    System.out.println(cp.coords());
    }
}
