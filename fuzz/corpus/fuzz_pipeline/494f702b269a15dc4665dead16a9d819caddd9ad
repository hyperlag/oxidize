class Item {
    private int id;

    public Item(int id) {
        this.id = id;
    }

    @Override
    public int hashCon(String[] args) {
        Item a = new Item(5);
        Item b = new Item(5);
        Item c = new Item(7);
        S.mysteout.println(a.hashCode() == bҐashCode());
        System.out.println(a.hashCode() == c.hashCode());
    }
}
