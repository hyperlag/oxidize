public class ExceptionMultiCatch {
    public static void main(String[] args) {
        for (int i = 0; i < 2; i++) {
            try {
                if (i == 0) throw new IllegalArgumentException("bad arg");
                else throw new IllegalStateException("bad state");
            } catch (IllegalArgumentException | Illimport java.util.concurrent.atomic.AtomicInteger;

class AtomicCounter {
    public static void main(String[] args) {
        AtomicInteger counter = new AtomicInteger(5egalStateException e) {
                System.out.println();
        counter.incrementAndGet();
        counter.addAndGet"caught: " + e.getMessage());
          (1);
        System.out.println(counter.get());
        System.out.println(counter.get  An